/**
 * CSS variable references for brand colors
 * Use these instead of hardcoding 'var(--accent-1)' in each component
 */
export const ACCENT_1 = 'var(--accent-1)';
export const ACCENT_2 = 'var(--accent-2)';

// Alias for single accent usage
export const ACCENT = ACCENT_1;
